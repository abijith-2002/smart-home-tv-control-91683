# Project Repository

Smart Home Android TV app lives in `android_tv_frontend`.

Build:
```bash
cd android_tv_frontend
./gradlew :app:installDebug
```

Use a TV emulator or device; navigate via D-Pad. Home shows Devices with count, cards toggle with DPAD_CENTER, and Activity/Settings show headings.