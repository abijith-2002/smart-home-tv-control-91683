<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    android:orientation="horizontal"
    android:focusable="true"
    android:focusableInTouchMode="true"
    tools:context=".MainActivity">

    <!-- Sidebar -->
    <LinearLayout
        android:id="@+id/sidebar"
        android:layout_width="96dp"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingTop="32dp"
        android:paddingBottom="32dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:background="@color/colorSurface"
        android:elevation="6dp">

        <ImageView
            android:id="@+id/navHomeIcon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center_horizontal"
            android:focusable="true"
            android:clickable="true"
            android:contentDescription="@string/nav_dashboard"
            android:padding="8dp"
            android:nextFocusRight="@id/contentContainer"
            android:src="@drawable/ic_nav_home" />

        <View
            android:layout_width="match_parent"
            android:layout_height="24dp" />

        <ImageView
            android:id="@+id/navActivityIcon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center_horizontal"
            android:focusable="true"
            android:clickable="true"
            android:contentDescription="@string/nav_activity"
            android:padding="8dp"
            android:nextFocusRight="@id/contentContainer"
            android:src="@drawable/ic_nav_activity" />

        <View
            android:layout_width="match_parent"
            android:layout_height="24dp" />

        <ImageView
            android:id="@+id/navSettingsIcon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center_horizontal"
            android:focusable="true"
            android:clickable="true"
            android:contentDescription="@string/nav_settings"
            android:padding="8dp"
            android:nextFocusRight="@id/contentContainer"
            android:src="@drawable/ic_nav_settings" />
    </LinearLayout>

    <!-- Content -->
    <FrameLayout
        android:id="@+id/contentContainer"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:padding="24dp"
        android:nextFocusLeft="@id/navHomeIcon" />
</LinearLayout>
