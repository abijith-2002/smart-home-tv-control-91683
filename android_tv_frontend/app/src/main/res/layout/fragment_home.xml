<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:clipToPadding="false"
    android:clipChildren="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/margin_large"
        android:clipToPadding="false"
        android:clipChildren="false">

        <!-- Header with Icon and Title -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/margin_xlarge"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <FrameLayout
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:background="@android:color/transparent">

                <ImageView
                    android:layout_width="60dp"
                    android:layout_height="60dp"
                    android:layout_gravity="center"
                    android:background="@null"
                    android:adjustViewBounds="true"
                    android:contentDescription="@string/smarthome"
                    android:scaleType="center"
                    app:srcCompat="@drawable/home_app_logo_24" />
            </FrameLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_large"
                android:text="@string/smarthome"
                android:textAppearance="@style/TextAppearance.Title" />
        </LinearLayout>

        <!-- Subheader with Devices count and Add button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/margin_large"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Left group: title + count -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/devices"
                    android:textAppearance="@style/TextAppearance.Header" />

                <FrameLayout
                    android:layout_width="@dimen/count_badge_size"
                    android:layout_height="@dimen/count_badge_size"
                    android:layout_marginStart="@dimen/count_badge_margin"
                    android:padding="@dimen/count_badge_padding"
                    android:background="@drawable/switch_on_bg">

                    <TextView
                        android:id="@+id/device_count"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:text="6"
                        android:textAppearance="@style/TextAppearance.CountBadge" />
                </FrameLayout>
            </LinearLayout>

            <!-- Add button on the right -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_add_device"
                style="@style/RoundedHeaderButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_large"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:text="@string/add_device"
                app:icon="@drawable/ic_add_24"
                app:iconGravity="textStart" />
        </LinearLayout>

        <!-- Device Cards Grid -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/devices_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:clipChildren="false"
            android:paddingTop="@dimen/margin_medium"
            android:paddingBottom="@dimen/margin_xlarge"
            android:paddingStart="@dimen/margin_medium"
            android:paddingEnd="@dimen/margin_medium" />
    </LinearLayout>
</ScrollView>
