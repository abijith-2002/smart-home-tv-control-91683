<?xml version="1.0" encoding="utf-8"?>
<!-- Subtle rounded pill that appears only when focused via selector usage -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused: low-alpha primary fill + subtle stroke, pill radius -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <!-- Very subtle fill to match Ocean Professional -->
            <solid android:color="@color/focus_glow" />
            <stroke android:width="1dp" android:color="@color/focus_color" />
        </shape>
    </item>

    <!-- Pressed: mirror focused visuals for consistency on click -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="@color/focus_glow" />
            <stroke android:width="1dp" android:color="@color/focus_color" />
        </shape>
    </item>

    <!-- Default: no background -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</selector>
